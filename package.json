{
    "name": "wrap-images",
    "version": "1.0.0",
    "description": "To use a jQuery plugin that once given a container it needs to find all the images inside the container and replace them for a new element, in this case &lt;figure&gt; that contains the image &lt;img&gt; and also a &lt;figcaption&gt; with the alt text or attribute of the image.",
    "main": "src/app.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "./node_modules/.bin/babel src --out-dir lib"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/NAVY1985/cardify.git"
    },
    "author": "Navy and Ada",
    "license": "ISC",
    "bugs": {
        "url": "https://github.com/NAVY1985/cardify/issues"
    },
    "homepage": "https://github.com/NAVY1985/cardify#readme",
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-preset-env": "^1.7.0"
    }
}